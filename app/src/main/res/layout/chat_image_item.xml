<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <com.makeramen.roundedimageview.RoundedImageView
        android:id="@+id/messageImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="@dimen/chat_image_size"
        android:maxHeight="@dimen/chat_image_size"
        android:adjustViewBounds="true"
        app:riv_border_color="@color/colorPrimary"
        app:riv_border_width="@dimen/chat_image_border_width"
        app:riv_corner_radius="@dimen/chat_image_border_radius"
        tools:ignore="ContentDescription"/>

    <TextView
        android:id="@+id/messageImageTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/messageImage"
        android:layout_alignEnd="@+id/messageImage"
        android:layout_marginEnd="@dimen/chat_time_margin_right"
        android:layout_marginBottom="@dimen/chat_time_margin_bottom"
        android:background="@color/colorMessageImageTime"/>

</RelativeLayout>